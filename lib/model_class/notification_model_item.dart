class Notification_model_item {
  String id, notification_name, notification_details, value, status, created_at;

  Notification_model_item(this.id, this.notification_name,
      this.notification_details, this.value, this.status, this.created_at);
}
